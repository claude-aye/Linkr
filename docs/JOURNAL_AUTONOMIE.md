# JOURNAL — Travail autonome CC

Une ligne par PR mergée, la plus récente en tête. C'est la piste d'audit à lire pour
rattraper une absence.

| Date | PR | Chantier | Migration | Ce qui a changé | Dette |
|---|---|---|---|---|---|
| 2026-08-20 | [#81](https://github.com/claude-aye/Linkr/pull/81) | D — avis, tranche 1b (front + 2 ajouts back) | — | **L'avis atteint l'écran, et la contrainte de lancement de #80 est LEVÉE** : écrire, voir, retirer, avec le **seuil de trois à l'écran**. **⚠️ LA RECON A TROUVÉ UN DÉFAUT RÉEL DANS #80, et le corriger est la partie la plus importante de la tranche** : `POST /reviews` exigeait `status === 'COMPLETED'`, or le cron horaire d'auto-libération capture le solde après `PLATFORM_AUTO_RELEASE_HOURS` (**72 h**) et le webhook fait passer en `PAID` — **le client qui attendait trois jours ne pouvait plus JAMAIS évaluer**, 409 définitif, dans la fenêtre exacte où les gens écrivent un avis. **Invisible au smoke de #80** parce que `COMPLETED → PAID` est inatteignable sans objets Stripe réels. La garde teste désormais **`completedAtUtc !== null`**, horodatage d'audit posé une fois et jamais effacé — l'encodage littéral de D-1. **Mesuré** : `PAID` → **201** ; `OPEN` et `CANCELLED` jamais complétées → **409**. **Décision humaine D-6 rendue sur la PR** : une demande **`REFUNDED` après complétion reste évaluable** (le service a eu lieu ; l'exclure biaiserait la moyenne **à la hausse** en retirant la parole à celui qui a le plus à dire ; le contrepoids est D-2). **Portée VERBATIM en §13.1 entrée 11** avec l'invariant généralisable : **toute garde écrite `status === X` sur un état TRAVERSÉ plutôt que TERMINAL expire en silence** — quand un horodatage d'audit existe pour le même fait, c'est lui qui porte la règle. **`GET /reviews/mine` n'est pas un confort** : le DTO public ne porte **ni** `serviceRequestId` **ni** id d'auteur (minimisation de #80, **maintenue**), donc rien ne permettait à un client de reconnaître son propre avis — l'id n'existait que dans la réponse du `POST`, et le droit de retrait (D-3) aurait duré **exactement une vue de page**. Un seul appel, joint par `serviceRequestId` côté serveur, jamais une requête par carte. **Surfaces** : `/requests` (un îlot client par job évaluable — le formulaire, ou l'avis + retrait via le `ConfirmDialog` partagé), `/providers/[id]` (bloc Avis, Server Component). **La moyenne n'est JAMAIS recalculée côté front** — elle arrive déjà gatée à trois (D-4, décidé en SQL). **Smoke navigateur réel** : 3 formulaires · publication → **1 seul POST** · retrait → **3 → 2 avis**, 1 seul DELETE · seuil dans les **deux sens** (3 → « 4,0 sur 5 · 3 avis » ; 2 → aucune moyenne + la ligne qui dit la règle) · état vide · **4 formulaires reviennent** après retrait de tous les avis (les créneaux libérés par l'index partiel, vus à l'écran). **Zéro débordement** à 320/375/414 px, **toutes** les vraies cibles à **44 px**. Contrat **+83/−0** et **+61/−0**, zéro suppression, **zéro `Record<string, never>`** — donc **zéro miroir et zéro cast**, sauf celui pré-existant sur `completedAtUtc` | **La contrainte de lancement de #80 est SOLDÉE** (le retrait est atteignable par son auteur). **Restent ouvertes** : écrire un avis **ne notifie personne** (`notifications` = chantier du collaborateur, non touché) · pas de pagination (50 avis/profil ↔ 100 avis propres ↔ 100 demandes, **plafonds solidaires**) · helper de statut partagé, inchangé · repli 502 des BFF **tutoyant**, mirroité pour ne pas faire diverger les 2 nouveaux relais de leurs frères |
| 2026-08-20 | [#80](https://github.com/claude-aye/Linkr/pull/80) | D — avis, tranche 1a (back) | `1780520000000` | **Le différenciateur du projet cesse d'être une intention** : table `reviews`, écriture, **suppression douce par l'auteur**, lecture agrégée. **D-1 rendu structurel** — client, état et prestataire sont **lus dans la demande, jamais dans le corps**, donc un faux avis ne peut pas exister et **aucune modération n'est à inventer**. **La réponse du prestataire est une COLONNE posée dès maintenant** (D-2) : « au plus une réponse » et « la suppression emporte la réponse » (D-3) deviennent des **faits du schéma** — **aucune cascade à écrire, donc aucune à oublier**, et la dette « suppression douce en cascade sur 2 tables sur 20 » **ne grossit pas**. **L'index unique partiel est exactement la forme du verrou session 4** (`ux_psc_provider_category_active`) — **et ici c'est la bonne, parce que la sortie existe** : `DELETE /reviews/:id` est livré dans la MÊME PR, donc « supprimer puis réécrire » remplace la modification interdite (mesuré : 409 doublon → 204 → **201 sur la même demande**). **Seuil de trois (D-4) appliqué EN SQL** : sous 3 avis vivants la moyenne vaut `null` **sur le fil**, le front n'a rien à masquer (2→`null`, 3→`4.33`, 4→`4`, retour à `null` après suppressions). **La recon a contredit le plan deux fois** : le patron d'autorisation à réutiliser n'était **pas** `getById` (contournement ADMIN → un admin aurait publié un avis **sous son propre nom** sur la transaction d'un tiers) mais celui de `confirmCompletion` ; et **le `CHECK` ne rejette PAS `3.5`** — PostgreSQL le **coerce en `smallint` (arrondi à `4`) avant que le `CHECK` s'exécute**, mesuré par `INSERT` brut, seul `@IsInt()` le rattrape. **Nom d'auteur masqué au mapper** (« Carol R. », `display_name` délibérément inutilisé — texte libre). **Lecture AUTHENTIFIÉE, `@Public()` retiré** sur arbitrage humain : son consommateur est privé, donc une route publique n'aurait **aucun appelant** — l'ouverture appartient au chantier « `discover` public », **avec** le limiteur et le `trust proxy`. **2ᵉ usager de `common/rate-limit/`** (10/h par appelant, 429 + `Retry-After`, un refus n'écrit rien). Contrat **+247/−0** et **+224/−0**, **zéro `Record<string, never>`**. `up → down → up` exercé **avec données préexistantes**, comptes identiques de part et d'autre | **4 créées** : (1) ⚠️ **contrainte de lancement — pas d'avis en production avant la tranche 1b** (le schéma permet le retrait, l'interface non : `DELETE` n'est atteignable qu'au `curl`) — **soldée par #81** ; (2) écrire un avis **ne notifie personne** (`notifications` = chantier du collaborateur, non touché) ; (3) pas de pagination au-delà de 50 ; (4) `updateReturningRows()` recopié une **6ᵉ** fois. **Conséquence assumée, pas une dette** : FK en `ON DELETE RESTRICT` ⇒ une demande `COMPLETED` portant un avis, et le prestataire qu'il nomme, ne peuvent plus jamais être supprimés en dur |
| 2026-08-20 | [#79](https://github.com/claude-aye/Linkr/pull/79) | Atteindre `COMPLETED` (hors file, brief humain) | — | **La première demande `COMPLETED` de l'histoire du projet**, et avec elle le déblocage du chantier D (un avis suppose une transaction terminée). Bloquée depuis la session 4 par `assertPayable` → 409, `stripe_connect_accounts` à 0 ligne. **La recon a contredit le brief sur deux points** : (1) le binaire « faut-il un appel Stripe réel ? » n'a pas de réponse unique — l'état et l'argent sont séparés par un `commit`, `assertPayable` est une vérification de **données pures** tandis que `captureDeposit` part **après** le commit vers `api.stripe.com`, donc un **502 laisse la demande `ASSIGNED` quand même** ; (2) il faut **deux** lignes, pas une — la garde a une seconde moitié (moyen de paiement du client) avec son propre 409, mesuré. Livrable : **un seul fichier**, un seeder idempotent à clés naturelles qui écrit la donnée que la garde réclame et pousse **trois** demandes, **une par état** (`ASSIGNED`/`IN_PROGRESS`/`COMPLETED`) — tout pousser à `COMPLETED` aurait laissé `JobPipelineAction` sans rien à rendre. **Salle blanche** : `0/0/0/0/0` → `1/1/3/1/3`. **Chemin rouge intact** : un autre prestataire sans ligne Connect reçoit **toujours son 409**, demande **`OPEN`**. **Zéro ligne de logique de paiement modifiée** | **0 créée**, 1 **soldée** (« `JobCard` jamais smoké », ouverte depuis la phase F — « Démarrer », « Compléter » et l'auto-annulation sur `COMPLETED` vus au navigateur). ⚠️ **Relevée, non corrigée** : un 502 de capture laisse la demande `ASSIGNED` **sans dépôt** — arbitrage humain, toute correction touche la capture ⇒ arrêt dur. Conséquences assumées : dépôts `FAILED` **par conception dans tout environnement**, `COMPLETED → PAID` toujours inatteignable |
| 2026-08-19 | [#78](https://github.com/claude-aye/Linkr/pull/78) | Solde de la file (hors file, brief humain) | — | **La file ⛔ est vidée.** `POST /demand-signals` est plafonné à **20 requêtes / 15 min par appelant** par un mécanisme **réutilisable** (`common/rate-limit/` : décorateur `@RateLimit` portant le budget au site d'appel + garde en provider local, convention `AdminGuard`), taillé pour le `discover` public à venir. **Mesuré** : 25 tentatives → 20 × 204 puis 5 × 429 `Retry-After: 900`, et en base **11 → 31** — les refus n'écrivent **rien** ; bob passe à l'instant où carol est bloquée (par appelant, pas global) ; la route admin voisine, non décorée, reste inerte (5×200). **En mémoire, pas Redis** — un limiteur adossé au cache doit choisir entre échouer ouvert (aucune protection sous tension) et échouer fermé (route morte sur panne de cache). **§13.1 gagne 2 entrées pour 3 invariants** (« offert ≠ résolvable » · l'absence d'identifiant de `demand_signals` sur les deux couches, texte porté tel quel), et le **mandat §3.9 est amendé** pour permettre ces ajouts. L'écran, audité, était **déjà conforme** — le corollaire « des recherches, jamais des personnes » y est néanmoins porté, et un commentaire qui confondait une recherche avec une personne est corrigé | **0 créée**, 1 **soldée** (limitation de débit). ⛔ **File vide.** Contreparties écrites : compte **par processus** (réplication ⇒ budgets multipliés, en silence) · fenêtre fixe ⇒ 2 × `limit` à la couture · `trust proxy` absent, à activer le jour du `discover` public |
| 2026-08-19 | [#77](https://github.com/claude-aye/Linkr/pull/77) | Lecture de la carte de la demande (hors file, brief humain) | — | `demand_signals` cesse d'être **illisible** : `GET /admin/demand-signals` **agrégé en SQL** (métier × secteur, période **tronquée au jour** en UTC) + écran `/admin/demand-signals`, **ADMIN seul** (401 anonyme / 403 non-ADMIN / 200 admin, mesurés). **Aucune ligne individuelle ne quitte le serveur** — `created_at_utc` est précis à la microseconde, donc une clé de corrélation : le regroupement se fait en base, et **aucune méthode de repository ne rend une ligne**. Secteur affiché **brut** + lien de carte externe (zéro appel, **aucune épingle** — une épingle revendique un point, c'est une cellule d'≈1 km). 4 états distincts, dont « aucune recherche infructueuse » (bonne nouvelle) **jamais confondu** avec « la lecture a échoué » (panne). 11 lignes brutes → 3 lignes servies | 1 créée (aucun index des consoles admin : les deux s'atteignent par URL seulement). 1 **soldée** (la carte était écrite mais illisible). ⛔ 4 décisions en attente — la **limitation de débit sur l'écriture monte en priorité** maintenant qu'une décision de recrutement s'appuiera sur ces chiffres |
| 2026-08-19 | [#76](https://github.com/claude-aye/Linkr/pull/76) | H-2a | `1780510000000` | Une recherche restée vide **laisse enfin une trace** : `demand_signals` = *métier + secteur grossier + date*, écrite par une **mutation POST explicite** (jamais un effet de bord de rendu — un préchargement ne peut pas la déclencher, prouvé au réseau). La table ne porte **aucun identifiant** — pas de `user_id` même nullable, pas d'IP, pas d'empreinte de session : cette **absence** est ce qui garde la ligne hors du champ des renseignements personnels, donc conservable **sans horloge de rétention**. `numeric(_,2)` rend la grossièreté **structurelle** (Postgres arrondit à l'écriture). La revendication de zéro est **re-vérifiée** avec le prédicat d'éligibilité de `discover` lui-même | 3 créées (carte illisible — **soldée par #77** · pas de limitation de débit — **soldée par #78** · le secteur est un arrondi faute de `locality`) |
| 2026-08-18 | [#75](https://github.com/claude-aye/Linkr/pull/75) | Fermeture entrée réglementée (hors file, brief humain) | — | Le MVP se lance en **métiers informels seulement** : les **deux** entrées client (tuiles de l'accueil + liste déroulante de `/recherche`) sont narrowies à `INFORMAL` par **un filtre nommé et partagé**, dérivé de la donnée ; l'URL réglementée **continue de rendre l'état vide honnête** (le filtre porte sur ce qui est *offert*, jamais sur ce qui est *résolvable* — contrefactuel mesuré). Catalogue, back et donnée **intacts**. En sus : « Mon tableau de bord » → **« Espace pro »**, qui fait tomber l'en-tête prestataire de **137 → 93 px dès 375 px** | 1 créée (périmètre de lancement en dur côté front, en attendant le *feature toggling*). ⛔ 3 décisions en attente (verrou du refus, conservation tranche 2, **inscrire « offert ≠ résolvable » en §13.1 ?**) |
| 2026-08-18 | [#74](https://github.com/claude-aye/Linkr/pull/74) | Fixture (hors file, brief humain) | — | **Première ligne `VERIFIED` de l'histoire de la base**, obtenue par le vrai chemin (déclaration → 2 documents → approbation ADMIN via la console) ; `discover` rend enfin un métier réglementé et le badge « ✓ Licence vérifiée » s'affiche pour la première fois ; seeder idempotent à clés naturelles | 0 créée. 2 **découvertes** et écrites : un refus verrouille le métier à vie (index ignore `verification_status`) · métiers réglementés indéclarables depuis l'interface. ⛔ 2 décisions en attente (verrou du refus, conservation tranche 2) |
| 2026-08-18 | [#73](https://github.com/claude-aye/Linkr/pull/73) | H-1 | — | L'accueil devient l'entrée **par métier** (7 tuiles → `/recherche?categoryId=`) ; l'état vide distingue « personne n'exerce ici » de « personne n'est encore vérifié » ; une recherche sans localisation dégrade explicitement au lieu de finir en cul-de-sac | 1 créée (le lien d'échappement de l'accueil disparaît avec le catalogue ; la nav le porte). ⛔ 1 décision en attente : durée de conservation, tranche 2 |
| 2026-08-17 | [#72](https://github.com/claude-aye/Linkr/pull/72) | G-1 | — | Nav `(app)` : cibles tactiles 20 px → 44 px (0 sous le plancher WCAG, contre 3/5), retour à la ligne épinglé, « Déconnexion » n'est plus seule sur sa ligne ; desktop inchangé | Aucune créée. Arbitrage : +18 px d'en-tête à 414 px, +10 px ≥ 768 px. ⛔ 1 décision de copie en attente |
