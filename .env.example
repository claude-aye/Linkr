# Copy to .env at project root. NEVER commit .env.
# Used by docker/docker-compose.yml to configure the local dev infrastructure.

POSTGRES_USER=linkr
POSTGRES_PASSWORD=linkr_dev_password
POSTGRES_DB=linkr

PGADMIN_DEFAULT_EMAIL=brou.aye.claude@outlook.com
PGADMIN_DEFAULT_PASSWORD=admin
